# Global ignores
.DS_Store
.env                 
.env.* 

.vscode/            

# OS generated files
Thumbs.db
Desktop.ini

# -------------------------
# Python specific ignores (for /server)
# -------------------------
server/__pycache__/
server/*.pyc
server/.pytest_cache/
server/venv/                   # Python virtual environment
server/.python-version
server/db.sqlite3              # Default Django SQLite database
server/static/                 # Collected static files (if Django serves them)
server/migrations/             # Exclude migration files generated by Django
!server/migrations/__init__.py 

# Django specific
server/manage.pyc

# -------------------------
# Node/React specific ignores (for /client)
# -------------------------
client/node_modules/
client/.vite/                  # Vite cache directory
client/dist/                   # Production build output
client/build/                  # Alternative build output
client/pnpm-lock.yaml          # Use if you prefer to commit package-lock.json or yarn.lock

# Log files
client/*.log
client/npm-debug.log*
client/yarn-debug.log*
client/yarn-error.log*

# -------------------------
# Docker specific ignores
# -------------------------
docker-compose.override.yml # If you use an override file locally
.dockerignore               # This file itself is usually committed, but listing it here to be explicit if ever nested

# Logs and databases created by Docker volumes
# These are typically outside the repo or within named volumes
# but including if they accidentally map to repo
# For example, if you had a volume mapping a local directory for postgres data:
# /pgdata/

# -------------------------
# Other generated files
# -------------------------
*.log
*.sqlite3